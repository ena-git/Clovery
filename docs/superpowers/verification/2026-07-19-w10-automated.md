# W10 iOS 1.1.0 Automated Verification

**Verification date:** 2026-08-01 18:00 CST

**Tested commit:** `ff1b022` (`test(release): close automated iOS gates`)

**Release identity:** `1.1.0 (15)`

**Simulator:** iPhone 17 Pro, iOS 26.0.1, arm64

## Acceptance Result

All W10 gates that can run without a physical iPhone, App Store Connect, signed archive, TestFlight, production credentials, or production deployment pass on the tested commit. This evidence does not mark staging, physical-device, StoreKit Sandbox, TestFlight, App Store review, or production rollout as complete.

## Authoritative Command

```bash
CLOVERY_RELEASE_API_BASE_URL=https://api.clovery.cn \
  scripts/verify-ios-1.1.0.sh
```

Result: exit `0`. The script used a unique directory under `/private/tmp`, printed only an aggregate result, and removed its logs, API binary, DerivedData, XCResult, and Release build products on exit.

## Gate Results

| Gate | Command or scope | Exit | Result |
| --- | --- | ---: | --- |
| Stable toolchain | Xcode/Node/Go/xcrun availability and non-beta Xcode | 0 | PASS |
| Backend race suite | `GOCACHE=<temporary> go test -race ./...` | 0 | PASS |
| Backend build | `go build -o <temporary>/clovery-api ./cmd/api` | 0 | PASS |
| Upgrade operations | `v2/scripts/test-account-upgrade-operations.sh` | 0 | PASS |
| Staging smoke contract | `v2/scripts/test-ios-1.1.0-staging-smoke.sh` | 0 | PASS |
| Staging safety contracts | configuration, data-safety, and evidence shell tests | 0 | PASS |
| Static iOS contracts | HTML, P0 bridge, Swift bridge, and migration ZIP scripts | 0 | PASS |
| Release contracts | identity, privacy, evidence, repository hygiene, and script probes | 0 | PASS |
| Complete XCTest suite | production API URL on selected iOS Simulator | 0 | PASS |
| XCTest result audit | XCResult summary requires no failure and no skip | 0 | PASS |
| Unsigned Release build | generic iOS Simulator with signing disabled | 0 | PASS |

XCTest result: `222` passed, `0` failed, `0` skipped.

## Toolchain

| Component | Verified version |
| --- | --- |
| Xcode | 26.0.1 (`17A400`) |
| Swift | Apple Swift 6.2 (`swiftlang-6.2.0.19.9`) |
| Go | 1.26.5, darwin/arm64 |
| PostgreSQL client | 16.13, Homebrew |
| PostgreSQL server | 16.13, Homebrew |

The Go module continues to declare its repository-controlled language/toolchain requirements; the version above records the machine used for this acceptance run.

## Regression Closure

- Standalone bridge compilation now includes an isolated Vault snapshot fixture and verifies structured JSON escaping deterministically.
- Migration ZIP smoke compilation now includes the current content validator/model and supplies a stable migration identifier.
- P0 contract checks now follow the current injected `BoardStore` and `ApplicationRootView` architecture instead of stale singleton/source paths.
- The full XCTest assertion checks structured serialization semantics rather than relying on one source formatting line.
- The release verifier preserves failing exit codes, suppresses raw command logs, rejects non-production API overrides, and leaves no repository-local DerivedData.
- CI and the legacy verification entry point both delegate to the same `scripts/verify-ios-1.1.0.sh` authority.

## Explicitly Deferred Gates

The following remain release blockers and must not be represented as PASS until executed with the required external systems:

- real staging API, database, object storage, and Apple credential smoke;
- physical-device upgrade from App Store `1.0.3 (14)` with retained diary/photo data;
- physical Photos add-only, denial, Settings recovery, repeated save, and share behavior;
- StoreKit Sandbox purchase, restore, revocation, paid-user remediation, and second-device entitlement inheritance;
- signed device archive, Organizer validation/upload, TestFlight matrix, App Store Connect metadata, and phased production rollout.
