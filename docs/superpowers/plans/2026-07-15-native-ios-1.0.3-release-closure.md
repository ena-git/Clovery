# Clovery Native iOS 1.0.3 Release Closure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将现有原生 iOS 应用安全发布为 `1.0.3 (14)`，完成照片保存、StoreKit 权益、升级数据、迁移导出、真机、TestFlight 和 App Store 发布门禁后，再解锁 Flutter 工作流。

**Architecture:** 保留 Swift/SwiftUI + WKWebView 的 V1 数据格式和 App Store 升级身份，将 Photos 与 StoreKit 从 881 行的 `WebView.Coordinator` 中拆成可注入、可测试的独立模块。自动化门禁负责代码、版本、签名配置和迁移格式；真实设备、Sandbox 和 TestFlight 负责系统权限、跨设备权益和升级数据的最终验收。

**Tech Stack:** Swift 5、SwiftUI、WKWebView、Photos、StoreKit 2、Combine、XCTest、Shell、GitHub Actions macOS runner、App Store Connect、TestFlight。

**Design:** `docs/superpowers/specs/2026-07-15-native-ios-1.0.3-release-closure-design.md`

---

## 文件结构

### 新增生产文件

- `Clovery/PhotoLibrarySaver.swift`：封装 Photos 授权和图片资源写入，不处理 WebView 消息。
- `Clovery/ImageExportService.swift`：解析图片 Data URL，分别调用系统相册保存、系统分享和设置跳转能力。
- `Clovery/BoardStoreClient.swift`：封装 StoreKit 2 产品、交易验证、当前权益、同步和交易更新序列。
- `Clovery/Clovery.storekit`：仅供 Xcode/CI 使用的终身商品 StoreKit 测试配置。

### 新增测试文件

- `CloveryTests/PhotoLibrarySaverTests.swift`：验证授权、拒绝、无效图片和 Photos 写入失败。
- `CloveryTests/ImageExportServiceTests.swift`：验证保存、分享、无效 Data URL 和设置跳转路由。
- `CloveryTests/BoardStoreTests.swift`：验证购买、Pending、取消、恢复、重启权益和交易更新。
- `CloveryTests/StoreKitIntegrationTests.swift`：使用 `SKTestSession` 验证 Ask to Buy 批准后的真实 StoreKit 更新。
- `Tests/IOSReleaseIdentityTests.sh`：验证发布身份检查器存在且能够阻止配置漂移。

### 新增发布工具

- `scripts/verify-ios-release-config.sh`：验证主 App、Widget、Bundle ID、版本、App Group、iCloud 和相册权限文案。
- `scripts/verify-ios-v1.sh`：统一运行 HTML、桥接、迁移、XCTest 和 Release 构建。
- `scripts/verify-ios-release-evidence.sh`：发布前拒绝未完成、失败或阻塞的人工验收记录。
- `.github/workflows/verify-ios-v1.yml`：在 macOS CI 上执行原生 iOS 自动化门禁。
- `docs/release/ios-1.0.3-acceptance.md`：记录不含账号、凭据和用户内容的发布证据索引。

### 修改文件

- `Clovery/WebView.swift`：只保留消息路由，接入 `ImageExportService` 和结构化权益回调。
- `Clovery/BoardStore.swift`：使用可注入 `BoardStoreClient`，确保先交付权益再完成交易。
- `Clovery/NativeBridgeModels.swift`：增加恢复购买结果枚举。
- `Clovery/BridgeJavaScript.swift`：增加恢复购买结构化回调。
- `Clovery/CloveryApp.swift`：应用回到前台时刷新当前权益。
- `Clovery/Clover Diary.html`：增加相册权限恢复入口和购买/恢复/Pending 状态提示。
- `Clovery.xcodeproj/project.pbxproj`：将新增 Swift 文件加入 App target，保持版本和签名身份不变。
- `Clovery.xcodeproj/xcshareddata/xcschemes/Clovery.xcscheme`：为 Debug/Test 指定已提交的 StoreKit 配置。
- `scripts/test-v1-p0-contract.sh`：把模块化后的 Photos、StoreKit 和设置桥加入静态防回归检查。
- `docs/release/1.0.3-ios-release.md`：记录新执行顺序和最终门禁结果。
- `docs/release/w1-acceptance-2026-07-11.md`：删除“Flutter 后才做真机验收”的旧排期。
- `docs/superpowers/plans/2026-07-10-clovery-rebuild-program.md`：把原生 `1.0.3` 发布设为 Flutter 启动前置条件。
- `docs/superpowers/plans/2026-07-10-w1-v1-data-protection.md`：同步更新 W1 验收顺序。

---

### Task 1: 锁定发布身份和执行顺序

**Files:**
- Create: `Tests/IOSReleaseIdentityTests.sh`
- Create: `scripts/verify-ios-release-config.sh`
- Modify: `docs/release/1.0.3-ios-release.md`
- Modify: `docs/release/w1-acceptance-2026-07-11.md`
- Modify: `docs/superpowers/plans/2026-07-10-clovery-rebuild-program.md`
- Modify: `docs/superpowers/plans/2026-07-10-w1-v1-data-protection.md`

- [ ] **Step 1: 写发布身份检查器的失败测试**

创建 `Tests/IOSReleaseIdentityTests.sh`：

```sh
#!/bin/sh
set -eu

repository_root=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)
checker="$repository_root/scripts/verify-ios-release-config.sh"

if [ ! -x "$checker" ]; then
  echo "missing executable release identity checker" >&2
  exit 1
fi

"$checker"
```

Run:

```bash
chmod +x Tests/IOSReleaseIdentityTests.sh
Tests/IOSReleaseIdentityTests.sh
```

Expected: FAIL with `missing executable release identity checker`.

- [ ] **Step 2: 实现发布身份检查器**

创建 `scripts/verify-ios-release-config.sh`：

```sh
#!/bin/sh
set -eu

repository_root=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)
project="$repository_root/Clovery.xcodeproj"

build_settings() {
  xcodebuild -project "$project" -target "$1" -configuration Release -showBuildSettings 2>/dev/null
}

setting() {
  printf '%s\n' "$1" | awk -F ' = ' -v key="$2" '$1 ~ "^[[:space:]]*" key "$" { print $2; exit }'
}

assert_equal() {
  actual=$1
  expected=$2
  label=$3
  if [ "$actual" != "$expected" ]; then
    echo "$label mismatch: expected $expected, got $actual" >&2
    exit 1
  fi
}

app=$(build_settings Clovery)
widget=$(build_settings CloveryWidgetExtension)

assert_equal "$(setting "$app" MARKETING_VERSION)" "1.0.3" "app marketing version"
assert_equal "$(setting "$app" CURRENT_PROJECT_VERSION)" "14" "app build number"
assert_equal "$(setting "$app" PRODUCT_BUNDLE_IDENTIFIER)" "com.clovery.app" "app bundle id"
assert_equal "$(setting "$widget" MARKETING_VERSION)" "1.0.3" "widget marketing version"
assert_equal "$(setting "$widget" CURRENT_PROJECT_VERSION)" "14" "widget build number"
assert_equal "$(setting "$widget" PRODUCT_BUNDLE_IDENTIFIER)" "com.clovery.app.CloveryWidget" "widget bundle id"

grep -Fq 'INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription' "$project/project.pbxproj"
grep -Fq 'group.com.clovery.app' "$repository_root/Clovery/Clovery.entitlements"
grep -Fq 'iCloud.com.clovery.app' "$repository_root/Clovery/Clovery.entitlements"
grep -Fq 'group.com.clovery.app' "$repository_root/CloveryWidgetExtension.entitlements"

echo "iOS 1.0.3 release identity verified"
```

Run:

```bash
chmod +x scripts/verify-ios-release-config.sh
Tests/IOSReleaseIdentityTests.sh
```

Expected: PASS with `iOS 1.0.3 release identity verified`.

- [ ] **Step 3: 同步四份计划和发布文档的顺序**

在四份文档中统一写入以下规则，不改动 W2 根账户/Vault 技术设计：

```markdown
原生 iOS `1.0.3 (14)` 的自动化、升级数据、相册、迁移、StoreKit、TestFlight
和 App Store 发布门禁必须在 Flutter W3 开始前完成。旧文档中“W3/W4 后再完成
W1 真机验收”的排期由 2026-07-15 已批准方案 A 替代。
```

Run:

```bash
rg -n "Flutter W3 开始前完成" docs/release docs/superpowers/plans
```

Expected: 四份目标文档均出现新顺序，且没有文档继续声明“Flutter iOS 完成后再验证原生 V1”。

- [ ] **Step 4: 运行格式与身份检查**

Run:

```bash
git diff --check
Tests/IOSReleaseIdentityTests.sh
```

Expected: 两条命令退出码均为 `0`。

- [ ] **Step 5: 提交发布顺序与身份门禁**

```bash
git add Tests/IOSReleaseIdentityTests.sh scripts/verify-ios-release-config.sh docs/release docs/superpowers/plans
git commit -m "test: lock native iOS release identity"
```

---

### Task 2: 提取并测试 Photos 写入服务

**Files:**
- Create: `Clovery/PhotoLibrarySaver.swift`
- Create: `CloveryTests/PhotoLibrarySaverTests.swift`
- Modify: `Clovery.xcodeproj/project.pbxproj`

- [ ] **Step 1: 写授权和写入失败测试**

创建 `CloveryTests/PhotoLibrarySaverTests.swift`，使用依赖闭包控制 Photos 状态：

```swift
import Photos
import XCTest
@testable import Clovery

final class PhotoLibrarySaverTests: XCTestCase {
    private let validPNG = Data(base64Encoded:
        "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAusB9Y9Z4n8AAAAASUVORK5CYII="
    )!

    func testDeniedPermissionDoesNotAttemptWrite() {
        var writeCount = 0
        let saver = PhotoLibrarySaver(client: PhotoLibraryClient(
            authorizationStatus: { .denied },
            requestAuthorization: { completion in completion(.denied) },
            createAsset: { _, completion in writeCount += 1; completion(true, nil) }
        ))

        let result = expectation(description: "result")
        saver.savePNG(validPNG) { outcome in
            XCTAssertEqual(outcome, .permissionDenied)
            result.fulfill()
        }
        wait(for: [result], timeout: 1)
        XCTAssertEqual(writeCount, 0)
    }

    func testNotDeterminedThenAuthorizedWritesAsset() {
        var writeCount = 0
        let saver = PhotoLibrarySaver(client: PhotoLibraryClient(
            authorizationStatus: { .notDetermined },
            requestAuthorization: { completion in completion(.authorized) },
            createAsset: { _, completion in writeCount += 1; completion(true, nil) }
        ))

        let result = expectation(description: "result")
        saver.savePNG(validPNG) { outcome in
            XCTAssertEqual(outcome, .success)
            result.fulfill()
        }
        wait(for: [result], timeout: 1)
        XCTAssertEqual(writeCount, 1)
    }

    func testInvalidImageAndWriteFailureAreVisible() {
        let invalid = PhotoLibrarySaver(client: authorizedClient(writeSucceeds: true))
        let invalidResult = expectation(description: "invalid")
        invalid.savePNG(Data("not-an-image".utf8)) { outcome in
            XCTAssertEqual(outcome, .invalidImage)
            invalidResult.fulfill()
        }

        let failed = PhotoLibrarySaver(client: authorizedClient(writeSucceeds: false))
        let failedResult = expectation(description: "failed")
        failed.savePNG(validPNG) { outcome in
            XCTAssertEqual(outcome, .failed)
            failedResult.fulfill()
        }

        wait(for: [invalidResult, failedResult], timeout: 1)
    }

    private func authorizedClient(writeSucceeds: Bool) -> PhotoLibraryClient {
        PhotoLibraryClient(
            authorizationStatus: { .authorized },
            requestAuthorization: { completion in completion(.authorized) },
            createAsset: { _, completion in completion(writeSucceeds, nil) }
        )
    }
}
```

Run:

```bash
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' test
```

Expected: FAIL because `PhotoLibrarySaver` and `PhotoLibraryClient` do not exist.

- [ ] **Step 2: 实现独立 Photos 服务**

创建 `Clovery/PhotoLibrarySaver.swift`：

```swift
import Photos
import UIKit

protocol PhotoLibrarySaving {
    func savePNG(_ data: Data, completion: @escaping (PhotoSaveOutcome) -> Void)
}

struct PhotoLibraryClient {
    let authorizationStatus: () -> PHAuthorizationStatus
    let requestAuthorization: (@escaping (PHAuthorizationStatus) -> Void) -> Void
    let createAsset: (Data, @escaping (Bool, Error?) -> Void) -> Void

    static let live = PhotoLibraryClient(
        authorizationStatus: {
            PHPhotoLibrary.authorizationStatus(for: .addOnly)
        },
        requestAuthorization: { completion in
            PHPhotoLibrary.requestAuthorization(for: .addOnly, handler: completion)
        },
        createAsset: { data, completion in
            PHPhotoLibrary.shared().performChanges {
                let request = PHAssetCreationRequest.forAsset()
                request.addResource(with: .photo, data: data, options: nil)
            } completionHandler: { success, error in
                completion(success, error)
            }
        }
    )

}

final class PhotoLibrarySaver: PhotoLibrarySaving {
    private let client: PhotoLibraryClient

    init(client: PhotoLibraryClient = .live) {
        self.client = client
    }

    func savePNG(_ data: Data, completion: @escaping (PhotoSaveOutcome) -> Void) {
        guard UIImage(data: data) != nil else {
            completion(.invalidImage)
            return
        }

        handle(client.authorizationStatus(), data: data, completion: completion)
    }

    private func handle(
        _ status: PHAuthorizationStatus,
        data: Data,
        completion: @escaping (PhotoSaveOutcome) -> Void
    ) {
        switch status {
        case .authorized, .limited:
            client.createAsset(data) { success, _ in
                completion(success ? .success : .failed)
            }
        case .notDetermined:
            client.requestAuthorization { [weak self] updatedStatus in
                self?.handle(updatedStatus, data: data, completion: completion)
            }
        case .denied, .restricted:
            completion(.permissionDenied)
        @unknown default:
            completion(.failed)
        }
    }
}
```

- [ ] **Step 3: 将文件加入 App target 并运行测试**

在 `Clovery.xcodeproj/project.pbxproj` 中为 `PhotoLibrarySaver.swift` 添加 `PBXFileReference`、`PBXBuildFile`、Clovery group 条目和 Clovery Sources 条目。`CloveryTests` 使用文件系统同步组，无需逐文件添加测试引用。

Run:

```bash
rg -n "PhotoLibrarySaver.swift in Sources" Clovery.xcodeproj/project.pbxproj
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' test
```

Expected: `PhotoLibrarySaverTests` 全部通过。

- [ ] **Step 4: 提交 Photos 服务**

```bash
git add Clovery/PhotoLibrarySaver.swift CloveryTests/PhotoLibrarySaverTests.swift Clovery.xcodeproj/project.pbxproj
git commit -m "feat: isolate photo library saving"
```

---

### Task 3: 模块化图片导出并提供权限恢复入口

**Files:**
- Create: `Clovery/ImageExportService.swift`
- Create: `CloveryTests/ImageExportServiceTests.swift`
- Modify: `Clovery/WebView.swift:13`
- Modify: `Clovery/WebView.swift:298`
- Modify: `Clovery/WebView.swift:806`
- Modify: `Clovery/Clover Diary.html:7960`
- Modify: `Clovery/Clover Diary.html:8540`
- Modify: `CloveryTests/WebBridgeContractTests.swift`
- Modify: `scripts/test-v1-p0-contract.sh`
- Modify: `Clovery.xcodeproj/project.pbxproj`

- [ ] **Step 1: 写图片路由和设置跳转失败测试**

创建 `CloveryTests/ImageExportServiceTests.swift`：

```swift
import XCTest
@testable import Clovery

final class ImageExportServiceTests: XCTestCase {
    private let validDataURL = "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAusB9Y9Z4n8AAAAASUVORK5CYII="

    func testSaveRoutesToPhotoLibraryAndReturnsOutcome() {
        let photoLibrary = PhotoLibrarySpy(outcome: .success)
        let share = ImageShareSpy()
        let settings = AppSettingsSpy()
        let service = ImageExportService(
            photoLibrary: photoLibrary,
            sharePresenter: share,
            settingsOpener: settings
        )

        let result = expectation(description: "save")
        service.handle(action: "save", dataURL: validDataURL) { outcome in
            XCTAssertEqual(outcome, .success)
            result.fulfill()
        }
        wait(for: [result], timeout: 1)
        XCTAssertEqual(photoLibrary.savedImageCount, 1)
        XCTAssertEqual(share.sharedImageCount, 0)
    }

    func testShareAndSettingsUseSeparateSystemCapabilities() {
        let photoLibrary = PhotoLibrarySpy(outcome: .success)
        let share = ImageShareSpy()
        let settings = AppSettingsSpy()
        let service = ImageExportService(
            photoLibrary: photoLibrary,
            sharePresenter: share,
            settingsOpener: settings
        )

        service.handle(action: "share", dataURL: validDataURL) { _ in
            XCTFail("share must not report a photo-library save")
        }
        service.openSettings()

        XCTAssertEqual(photoLibrary.savedImageCount, 0)
        XCTAssertEqual(share.sharedImageCount, 1)
        XCTAssertEqual(settings.openCount, 1)
    }

    func testInvalidDataURLReturnsInvalidImage() {
        let service = ImageExportService(
            photoLibrary: PhotoLibrarySpy(outcome: .success),
            sharePresenter: ImageShareSpy(),
            settingsOpener: AppSettingsSpy()
        )
        let result = expectation(description: "invalid")
        service.handle(action: "save", dataURL: "data:text/plain;base64,QQ==") { outcome in
            XCTAssertEqual(outcome, .invalidImage)
            result.fulfill()
        }
        wait(for: [result], timeout: 1)
    }
}

private final class PhotoLibrarySpy: PhotoLibrarySaving {
    let outcome: PhotoSaveOutcome
    private(set) var savedImageCount = 0

    init(outcome: PhotoSaveOutcome) {
        self.outcome = outcome
    }

    func savePNG(_ data: Data, completion: @escaping (PhotoSaveOutcome) -> Void) {
        savedImageCount += 1
        completion(outcome)
    }
}

private final class ImageShareSpy: ImageSharing {
    private(set) var sharedImageCount = 0
    func sharePNG(_ data: Data) { sharedImageCount += 1 }
}

private final class AppSettingsSpy: AppSettingsOpening {
    private(set) var openCount = 0
    func open() { openCount += 1 }
}
```

Spy 只记录调用次数，不访问 Photos、系统相册或 `UIApplication`。

Run:

```bash
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' test
```

Expected: FAIL because `ImageExportService` does not exist.

- [ ] **Step 2: 实现图片导出服务**

创建 `Clovery/ImageExportService.swift`，接口固定为：

```swift
import UIKit

protocol ImageSharing {
    func sharePNG(_ data: Data)
}

protocol AppSettingsOpening {
    func open()
}

protocol ImageExporting {
    func handle(
        action: String,
        dataURL: String,
        saveCompletion: @escaping (PhotoSaveOutcome) -> Void
    )
    func openSettings()
}

final class ImageExportService: ImageExporting {
    private let photoLibrary: PhotoLibrarySaving
    private let sharePresenter: ImageSharing
    private let settingsOpener: AppSettingsOpening

    init(
        photoLibrary: PhotoLibrarySaving = PhotoLibrarySaver(),
        sharePresenter: ImageSharing = SystemImageSharePresenter(),
        settingsOpener: AppSettingsOpening = SystemAppSettingsOpener()
    ) {
        self.photoLibrary = photoLibrary
        self.sharePresenter = sharePresenter
        self.settingsOpener = settingsOpener
    }

    func handle(
        action: String,
        dataURL: String,
        saveCompletion: @escaping (PhotoSaveOutcome) -> Void
    ) {
        let prefix = "data:image/png;base64,"
        guard dataURL.hasPrefix(prefix),
              let data = Data(base64Encoded: String(dataURL.dropFirst(prefix.count))),
              UIImage(data: data) != nil else {
            if action == "save" { saveCompletion(.invalidImage) }
            return
        }

        switch action {
        case "save":
            photoLibrary.savePNG(data, completion: saveCompletion)
        case "share":
            sharePresenter.sharePNG(data)
        default:
            break
        }
    }

    func openSettings() {
        settingsOpener.open()
    }
}

final class SystemImageSharePresenter: ImageSharing {
    func sharePNG(_ data: Data) {
        let url = FileManager.default.temporaryDirectory
            .appendingPathComponent("clovery-\(UUID().uuidString).png")
        do {
            try data.write(to: url, options: .atomic)
        } catch {
            return
        }

        DispatchQueue.main.async {
            guard let rootViewController = UIApplication.shared.connectedScenes
                .compactMap({ $0 as? UIWindowScene })
                .flatMap(\.windows)
                .first(where: \.isKeyWindow)?
                .rootViewController else { return }

            let controller = UIActivityViewController(
                activityItems: [url],
                applicationActivities: nil
            )
            controller.popoverPresentationController?.sourceView = rootViewController.view
            rootViewController.present(controller, animated: true)
        }
    }
}

final class SystemAppSettingsOpener: AppSettingsOpening {
    func open() {
        guard let url = URL(string: UIApplication.openSettingsURLString) else { return }
        DispatchQueue.main.async {
            UIApplication.shared.open(url)
        }
    }
}
```

- [ ] **Step 3: 将 WebView 收敛为消息路由**

在 `WebView.Coordinator` 中注入服务：

```swift
private let imageExporter: ImageExporting

init(
    photoStore: PhotoStoring = PhotoStore(),
    imageExporter: ImageExporting = ImageExportService()
) {
    self.photoStore = photoStore
    self.imageExporter = imageExporter
    super.init()
}
```

把 `shareImage` 和新 `openAppSettings` handler 改为：

```swift
} else if message.name == "shareImage" {
    guard let body = message.body as? [String: Any],
          let action = body["action"] as? String,
          let dataURL = body["dataURL"] as? String else { return }
    imageExporter.handle(action: action, dataURL: dataURL) { [weak self] outcome in
        self?.reportPhotoSave(outcome)
    }
} else if message.name == "openAppSettings" {
    imageExporter.openSettings()
```

删除 `WebView.swift` 内旧的 `handleShareImage` 和 `saveImageDataToPhotos`，并注册：

```swift
config.userContentController.add(context.coordinator, name: "openAppSettings")
```

- [ ] **Step 4: 在拒绝权限提示中增加设置按钮**

在 `Clover Diary.html` 的相册保存错误区域中使用以下结构：

```jsx
{saveError === 'permissionDenied' && (
  <button type="button" onClick={() => {
    window.webkit?.messageHandlers?.openAppSettings?.postMessage({});
  }} style={{
    marginTop:6, border:0, background:'transparent', color:theme.accentDark,
    textDecoration:'underline', fontSize:13, fontWeight:600
  }}>
    {lang==='zh' ? '前往系统设置' : lang==='ja' ? '設定を開く' : lang==='ko' ? '설정 열기' : 'Open Settings'}
  </button>
)}
```

修改 `WebBridgeContractTests` 和 `test-v1-p0-contract.sh`，必须验证 `openAppSettings` 已注册、HTML 调用该 handler，且 `WebView.swift` 不再直接出现 `PHPhotoLibrary.shared().performChanges`。

- [ ] **Step 5: 将文件加入 target 并运行照片门禁**

Run:

```bash
node scripts/validate-v1-html.cjs
scripts/test-v1-p0-contract.sh
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' test
```

Expected: Babel、P0 合同、`PhotoLibrarySaverTests`、`ImageExportServiceTests` 和桥接测试全部通过。

- [ ] **Step 6: 提交图片导出模块**

```bash
git add Clovery/ImageExportService.swift Clovery/WebView.swift 'Clovery/Clover Diary.html' CloveryTests scripts/test-v1-p0-contract.sh Clovery.xcodeproj/project.pbxproj
git commit -m "fix: make photo library saves recoverable"
```

---

### Task 4: 将 StoreKit 2 封装为可测试客户端

**Files:**
- Create: `Clovery/BoardStoreClient.swift`
- Create: `Clovery/Clovery.storekit`
- Create: `CloveryTests/BoardStoreTests.swift`
- Create: `CloveryTests/StoreKitIntegrationTests.swift`
- Modify: `Clovery/BoardStore.swift`
- Modify: `Clovery/NativeBridgeModels.swift`
- Modify: `Clovery.xcodeproj/project.pbxproj`
- Modify: `Clovery.xcodeproj/xcshareddata/xcschemes/Clovery.xcscheme`

- [ ] **Step 1: 写权益、购买和恢复失败测试**

创建 `CloveryTests/BoardStoreTests.swift`，至少包含以下用例：

```swift
import XCTest
@testable import Clovery

@MainActor
final class BoardStoreTests: XCTestCase {
    func testRefreshRestoresVerifiedLifetimeEntitlement() async {
        let transaction = BoardTransaction.stub(productID: BoardStore.productID)
        let store = BoardStore(
            client: .stub(currentEntitlements: [transaction]),
            observesUpdates: false,
            refreshesOnInit: false
        )

        await store.refresh()

        XCTAssertTrue(store.isUnlocked)
    }

    func testSuccessfulPurchaseUnlocksBeforeFinishingTransaction() async {
        var store: BoardStore!
        var unlockedWhenFinished = false
        let transaction = BoardTransaction(
            productID: BoardStore.productID,
            revocationDate: nil,
            finishOperation: { @MainActor in unlockedWhenFinished = store.isUnlocked }
        )
        store = BoardStore(
            client: .stub(purchaseResult: .success(transaction)),
            observesUpdates: false,
            refreshesOnInit: false
        )

        let outcome = await store.purchase()

        XCTAssertEqual(outcome, .success)
        XCTAssertTrue(store.isUnlocked)
        XCTAssertTrue(unlockedWhenFinished)
    }

    func testCancelledAndPendingPurchasesDoNotUnlock() async {
        for result in [BoardClientPurchaseResult.cancelled, .pending] {
            let store = BoardStore(
                client: .stub(purchaseResult: result),
                observesUpdates: false,
                refreshesOnInit: false
            )
            let outcome = await store.purchase()
            XCTAssertFalse(store.isUnlocked)
            XCTAssertTrue(outcome == .cancelled || outcome == .pending)
        }
    }

    func testRestoreDistinguishesRestoredNotFoundAndFailed() async {
        let restored = BoardStore(
            client: .stub(currentEntitlements: [.stub(productID: BoardStore.productID)]),
            observesUpdates: false,
            refreshesOnInit: false
        )
        XCTAssertEqual(await restored.restore(), .restored)

        let missing = BoardStore(
            client: .stub(),
            observesUpdates: false,
            refreshesOnInit: false
        )
        XCTAssertEqual(await missing.restore(), .notFound)

        let failed = BoardStore(
            client: .stub(syncError: StoreClientTestError.failed),
            observesUpdates: false,
            refreshesOnInit: false
        )
        XCTAssertEqual(await failed.restore(), .failed)
    }

    func testApprovedTransactionUpdateUnlocksAndFinishes() async {
        var continuation: AsyncStream<BoardTransaction>.Continuation!
        let updates = AsyncStream<BoardTransaction> { continuation = $0 }
        let finished = expectation(description: "finished")
        let store = BoardStore(
            client: .stub(updates: updates),
            observesUpdates: true,
            refreshesOnInit: false
        )

        continuation.yield(BoardTransaction(
            productID: BoardStore.productID,
            revocationDate: nil,
            finishOperation: { finished.fulfill() }
        ))

        await fulfillment(of: [finished], timeout: 1)
        XCTAssertTrue(store.isUnlocked)
        continuation.finish()
    }
}
```

Run:

```bash
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' test
```

Expected: FAIL because `BoardStoreClient`, `BoardTransaction` and `BoardRestoreOutcome` do not exist.

- [ ] **Step 2: 定义 StoreKit 客户端领域模型**

创建 `Clovery/BoardStoreClient.swift`：

```swift
import OSLog
import StoreKit

struct BoardTransaction: Sendable {
    let productID: String
    let revocationDate: Date?
    let finishOperation: @Sendable () async -> Void

    func finish() async {
        await finishOperation()
    }
}

enum BoardClientPurchaseResult: Sendable {
    case success(BoardTransaction)
    case cancelled
    case pending
    case failed
}

struct BoardStoreClient: Sendable {
    let currentEntitlements: @Sendable () async -> [BoardTransaction]
    let purchase: @Sendable (_ productID: String) async -> BoardClientPurchaseResult
    let displayPrice: @Sendable (_ productID: String) async -> String?
    let sync: @Sendable () async throws -> Void
    let updates: @Sendable () -> AsyncStream<BoardTransaction>
}

private let boardStoreClientLogger = Logger(
    subsystem: Bundle.main.bundleIdentifier ?? "com.clovery.app",
    category: "StoreKit"
)

private extension BoardTransaction {
    init(storeKitTransaction: Transaction) {
        productID = storeKitTransaction.productID
        revocationDate = storeKitTransaction.revocationDate
        finishOperation = { await storeKitTransaction.finish() }
    }
}

extension BoardStoreClient {
    static var live: BoardStoreClient {
        BoardStoreClient(
            currentEntitlements: {
                var transactions: [BoardTransaction] = []
                for await result in Transaction.currentEntitlements {
                    switch result {
                    case .verified(let transaction):
                        transactions.append(BoardTransaction(storeKitTransaction: transaction))
                    case .unverified(_, let error):
                        boardStoreClientLogger.error(
                            "Unverified entitlement: \(String(describing: error), privacy: .public)"
                        )
                    }
                }
                return transactions
            },
            purchase: { productID in
                do {
                    let products = try await Product.products(for: [productID])
                    guard let product = products.first(where: { $0.id == productID }) else {
                        boardStoreClientLogger.error("Product unavailable: \(productID, privacy: .public)")
                        return .failed
                    }

                    var options: Set<Product.PurchaseOption> = []
                    #if DEBUG
                    if ProcessInfo.processInfo.arguments.contains("-CloverySimulateAskToBuy") {
                        options.insert(.simulatesAskToBuyInSandbox(true))
                    }
                    #endif

                    switch try await product.purchase(options: options) {
                    case .success(let result):
                        switch result {
                        case .verified(let transaction):
                            return .success(BoardTransaction(storeKitTransaction: transaction))
                        case .unverified(_, let error):
                            boardStoreClientLogger.error(
                                "Purchase verification failed: \(String(describing: error), privacy: .public)"
                            )
                            return .failed
                        }
                    case .userCancelled:
                        return .cancelled
                    case .pending:
                        return .pending
                    @unknown default:
                        return .failed
                    }
                } catch {
                    boardStoreClientLogger.error(
                        "Purchase failed: \(error.localizedDescription, privacy: .public)"
                    )
                    return .failed
                }
            },
            displayPrice: { productID in
                do {
                    return try await Product.products(for: [productID])
                        .first(where: { $0.id == productID })?
                        .displayPrice
                } catch {
                    boardStoreClientLogger.error(
                        "Price request failed: \(error.localizedDescription, privacy: .public)"
                    )
                    return nil
                }
            },
            sync: {
                try await AppStore.sync()
            },
            updates: {
                AsyncStream { continuation in
                    let task = Task {
                        for await result in Transaction.updates {
                            guard !Task.isCancelled else { break }
                            switch result {
                            case .verified(let transaction):
                                continuation.yield(
                                    BoardTransaction(storeKitTransaction: transaction)
                                )
                            case .unverified(_, let error):
                                boardStoreClientLogger.error(
                                    "Transaction update verification failed: \(String(describing: error), privacy: .public)"
                                )
                            }
                        }
                        continuation.finish()
                    }
                    continuation.onTermination = { _ in task.cancel() }
                }
            }
        )
    }
}
```

`-CloverySimulateAskToBuy` 只在 `#if DEBUG` 中加入 Sandbox Ask to Buy 选项；Release 构建没有该分支。`AppStore.sync()` 仍只由用户点击“恢复购买”触发。

- [ ] **Step 3: 重写 BoardStore 的状态顺序**

将 `BoardStore` 初始化和核心方法固定为：

```swift
@MainActor
final class BoardStore: ObservableObject {
    static let productID = "com.clovery.app.board.lifetime"
    static let shared = BoardStore()

    @Published private(set) var isUnlocked = false

    private let client: BoardStoreClient
    private var updatesTask: Task<Void, Never>?

    init(
        client: BoardStoreClient = .live,
        observesUpdates: Bool = true,
        refreshesOnInit: Bool = true
    ) {
        self.client = client
        if observesUpdates {
            updatesTask = Task { [weak self] in
                await self?.observeTransactionUpdates()
            }
        }
        if refreshesOnInit {
            Task { [weak self] in await self?.refresh() }
        }
    }

    func refresh() async {
        let transactions = await client.currentEntitlements()
        isUnlocked = transactions.contains {
            $0.productID == Self.productID && $0.revocationDate == nil
        }
    }

    func purchase() async -> BoardPurchaseOutcome {
        switch await client.purchase(Self.productID) {
        case .success(let transaction)
            where transaction.productID == Self.productID && transaction.revocationDate == nil:
            isUnlocked = true
            await transaction.finish()
            return .success
        case .cancelled:
            return .cancelled
        case .pending:
            return .pending
        case .success, .failed:
            return .failed
        }
    }

    func fetchDisplayPrice() async -> String? {
        await client.displayPrice(Self.productID)
    }

    func restore() async -> BoardRestoreOutcome {
        do {
            try await client.sync()
            await refresh()
            return isUnlocked ? .restored : .notFound
        } catch {
            return .failed
        }
    }

    private func observeTransactionUpdates() async {
        for await transaction in client.updates() {
            guard !Task.isCancelled else { return }
            if transaction.productID == Self.productID && transaction.revocationDate == nil {
                isUnlocked = true
                await transaction.finish()
            } else {
                await refresh()
            }
        }
    }

    deinit {
        updatesTask?.cancel()
    }
}
```

在 `NativeBridgeModels.swift` 增加：

```swift
enum BoardRestoreOutcome: String, Sendable, Equatable {
    case restored
    case notFound
    case failed
}
```

同时为现有 `BoardPurchaseOutcome` 和 `PhotoSaveOutcome` 增加 `Equatable`，让状态机测试使用强类型断言而不是比较字符串。

- [ ] **Step 4: 为测试提供完全本地的 stub**

在 `BoardStoreTests.swift` 末尾加入：

```swift
private enum StoreClientTestError: Error {
    case failed
}

private extension BoardTransaction {
    static func stub(productID: String) -> BoardTransaction {
        BoardTransaction(
            productID: productID,
            revocationDate: nil,
            finishOperation: {}
        )
    }
}

private extension BoardStoreClient {
    static func stub(
        currentEntitlements: [BoardTransaction] = [],
        purchaseResult: BoardClientPurchaseResult = .failed,
        displayPrice: String? = "¥6.00",
        syncError: Error? = nil,
        updates: AsyncStream<BoardTransaction>? = nil
    ) -> BoardStoreClient {
        let updateStream = updates ?? AsyncStream { continuation in
            continuation.finish()
        }
        return BoardStoreClient(
            currentEntitlements: { currentEntitlements },
            purchase: { _ in purchaseResult },
            displayPrice: { _ in displayPrice },
            sync: {
                if let syncError { throw syncError }
            },
            updates: { updateStream }
        )
    }
}
```

Stub 测试不得连接 App Store 或依赖 Sandbox Apple Account。

- [ ] **Step 5: 创建 StoreKit 配置并自动验证 Ask to Buy**

在 Xcode 中创建 `Clovery/Clovery.storekit`，只包含一项本地测试商品：

| Field | Value |
| --- | --- |
| Reference Name | `Clovery Board Lifetime` |
| Product ID | `com.clovery.app.board.lifetime` |
| Type | Non-Consumable |
| Local test price | `6.00 CNY` |
| zh-Hans display name | `四叶草田终身版` |
| en display name | `Clovery Board Lifetime` |

把配置加入 Clovery App target 的 Copy Bundle Resources，并在共享 `Clovery.xcscheme` 的 Run 与 Test Options 中选择该配置。将 `CloveryTests` 的 `parallelizable` 改为 `NO`，因为 Apple 的 StoreKit 测试环境在同一进程中共享状态。

创建 `CloveryTests/StoreKitIntegrationTests.swift`：

```swift
import StoreKitTest
import XCTest
@testable import Clovery

@MainActor
final class StoreKitIntegrationTests: XCTestCase {
    private var session: SKTestSession!

    override func setUpWithError() throws {
        session = try SKTestSession(configurationFileNamed: "Clovery")
        session.disableDialogs = true
        session.clearTransactions()
        session.askToBuyEnabled = false
    }

    override func tearDown() {
        session.clearTransactions()
        session.resetToDefaultState()
        session = nil
    }

    func testAskToBuyApprovalUnlocksThroughTransactionUpdates() async throws {
        session.askToBuyEnabled = true
        let store = BoardStore(
            client: .live,
            observesUpdates: true,
            refreshesOnInit: false
        )

        XCTAssertEqual(await store.purchase(), .pending)
        XCTAssertFalse(store.isUnlocked)

        let pending = try XCTUnwrap(session.allTransactions().first {
            $0.productIdentifier == BoardStore.productID && $0.pendingAskToBuyConfirmation
        })
        try session.approveAskToBuyTransaction(identifier: pending.identifier)

        for _ in 0..<40 where !store.isUnlocked {
            try await Task.sleep(nanoseconds: 50_000_000)
        }
        XCTAssertTrue(store.isUnlocked)

        let relaunchedStore = BoardStore(
            client: .live,
            observesUpdates: false,
            refreshesOnInit: false
        )
        await relaunchedStore.refresh()
        XCTAssertTrue(relaunchedStore.isUnlocked)
    }
}
```

Run:

```bash
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:CloveryTests/StoreKitIntegrationTests test
```

Expected: purchase first returns `pending`; approval is delivered through `Transaction.updates`; a new store recovers the same non-consumable through `currentEntitlements`.

- [ ] **Step 6: 加入 App target 并运行 StoreKit 测试**

Run:

```bash
rg -n "BoardStoreClient.swift in Sources" Clovery.xcodeproj/project.pbxproj
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' test
```

Expected: `BoardStoreTests` 和 `StoreKitIntegrationTests` 全部通过，现有 JavaScript 和桥接测试不回归。

- [ ] **Step 7: 提交 StoreKit 客户端与状态机**

```bash
git add Clovery/BoardStoreClient.swift Clovery/BoardStore.swift Clovery/NativeBridgeModels.swift Clovery/Clovery.storekit CloveryTests/BoardStoreTests.swift CloveryTests/StoreKitIntegrationTests.swift Clovery.xcodeproj/project.pbxproj Clovery.xcodeproj/xcshareddata/xcschemes/Clovery.xcscheme
git commit -m "fix: make StoreKit entitlements recoverable"
```

---

### Task 5: 完成权益桥接、前台刷新和用户反馈

**Files:**
- Modify: `Clovery/BridgeJavaScript.swift`
- Modify: `Clovery/WebView.swift:93`
- Modify: `Clovery/WebView.swift:806`
- Modify: `Clovery/WebView.swift:870`
- Modify: `Clovery/CloveryApp.swift`
- Modify: `Clovery/Clover Diary.html:7841`
- Modify: `Clovery/Clover Diary.html:7850`
- Modify: `Clovery/Clover Diary.html:7804`
- Modify: `CloveryTests/JavaScriptCallbackTests.swift`
- Modify: `CloveryTests/WebBridgeContractTests.swift`
- Modify: `scripts/test-v1-p0-contract.sh`

- [ ] **Step 1: 写恢复结果和前台刷新失败测试**

扩展 `JavaScriptCallbackTests`：

```swift
func testRestoreResultUsesStructuredCallback() {
    XCTAssertEqual(
        BridgeJavaScript.boardRestoreResult(.notFound),
        #"window._boardRestoreResult?.("notFound");"#
    )
}
```

扩展 `WebBridgeContractTests`，验证：

```swift
XCTAssertTrue(webViewSource.contains("startObservingBoardStore()"))
XCTAssertTrue(appSource.contains("refreshBoardEntitlement()"))
XCTAssertTrue(html.contains("window._boardRestoreResult = (outcome) =>"))
XCTAssertTrue(html.contains("购买请求正在等待批准"))
XCTAssertTrue(html.contains("没有找到可恢复的购买记录"))
```

Run:

```bash
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' test
```

Expected: FAIL because bridge and lifecycle behavior are missing.

- [ ] **Step 2: 增加恢复购买结构化回调**

在 `BridgeJavaScript.swift` 增加：

```swift
static func boardRestoreResult(_ outcome: BoardRestoreOutcome) -> String {
    evaluateJSONCallback(name: "window._boardRestoreResult", payload: [outcome.rawValue])
}
```

把 WebView 的恢复 handler 改为同时回传恢复结果和最终权益：

```swift
let outcome = await BoardStore.shared.restore()
_ = try? await self.webView?.evaluateJavaScript(
    BridgeJavaScript.boardRestoreResult(outcome)
)
_ = try? await self.webView?.evaluateJavaScript(
    BridgeJavaScript.boardUnlockStatus(BoardStore.shared.isUnlocked)
)
```

- [ ] **Step 3: 监听权益变化并在前台刷新**

在 `WebView.Coordinator` 中使用 Combine：

```swift
private var boardEntitlementCancellable: AnyCancellable?

func startObservingBoardStore() {
    boardEntitlementCancellable = BoardStore.shared.$isUnlocked
        .removeDuplicates()
        .receive(on: RunLoop.main)
        .sink { [weak self] unlocked in
            self?.webView?.evaluateJavaScript(
                BridgeJavaScript.boardUnlockStatus(unlocked),
                completionHandler: nil
            )
        }
}
```

`makeUIView` 在绑定 `webView` 后调用 `startObservingBoardStore()`。在 `WebViewCoordinatorBridge` 增加：

```swift
func refreshBoardEntitlement() {
    Task { @MainActor in
        await BoardStore.shared.refresh()
    }
}
```

在 `CloveryApp` 中监听 `scenePhase`：

```swift
@Environment(\.scenePhase) private var scenePhase

ContentView()
    .ignoresSafeArea()
    .onChange(of: scenePhase) { phase in
        if phase == .active {
            WebViewCoordinatorBridge.shared.refreshBoardEntitlement()
        }
    }
```

- [ ] **Step 4: 完成 Pending、恢复成功、未找到和失败提示**

在 `ShareCardSheet` 增加 `purchaseNotice` 状态。`_boardPurchaseResult` 收到 `pending` 时显示“购买请求正在等待批准，批准后会自动解锁”；`_boardRestoreResult` 分别显示：

```javascript
window._boardRestoreResult = (outcome) => {
  setRestoring(false);
  if (outcome === 'restored') {
    setPurchaseNotice(lang==='zh' ? '购买已恢复' : 'Purchase restored');
    setPurchaseError(false);
  } else if (outcome === 'notFound') {
    setPurchaseNotice(lang==='zh' ? '没有找到可恢复的购买记录' : 'No restorable purchase was found');
  } else {
    setPurchaseNotice(null);
    setPurchaseError(true);
  }
};
```

组件卸载时删除 `_boardRestoreResult`。取消购买不显示错误；Pending 不解锁；恢复失败保留重试按钮。

- [ ] **Step 5: 运行全部 P0 和桥接测试**

Run:

```bash
node scripts/validate-v1-html.cjs
scripts/test-v1-p0-contract.sh
scripts/test-v1-bridge.sh
xcodebuild -project Clovery.xcodeproj -scheme Clovery -destination 'platform=iOS Simulator,name=iPhone 16' test
```

Expected: 全部通过；`BoardStoreTests` 证明权益先更新再 finish，HTML 测试证明 Pending 和恢复结果可见。

- [ ] **Step 6: 提交权益桥接**

```bash
git add Clovery/BridgeJavaScript.swift Clovery/WebView.swift Clovery/CloveryApp.swift 'Clovery/Clover Diary.html' CloveryTests scripts/test-v1-p0-contract.sh
git commit -m "fix: refresh purchase entitlements across launches"
```

---

### Task 6: 建立原生 iOS CI 与统一自动化入口

**Files:**
- Create: `scripts/verify-ios-v1.sh`
- Create: `.github/workflows/verify-ios-v1.yml`
- Modify: `README.md`

- [ ] **Step 1: 创建统一验证脚本**

创建 `scripts/verify-ios-v1.sh`：

```sh
#!/bin/sh
set -eu

repository_root=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)
destination=${CLOVERY_IOS_DESTINATION:-platform=iOS Simulator,name=iPhone 16}

cd "$repository_root"
node scripts/validate-v1-html.cjs
scripts/test-v1-p0-contract.sh
scripts/test-v1-bridge.sh
scripts/test-migration-zip.sh
scripts/verify-ios-release-config.sh

xcodebuild \
  -project Clovery.xcodeproj \
  -scheme Clovery \
  -destination "$destination" \
  -derivedDataPath build/DerivedData-v1 \
  test CODE_SIGNING_ALLOWED=NO

xcodebuild \
  -project Clovery.xcodeproj \
  -scheme Clovery \
  -configuration Release \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath build/DerivedData-v1-release \
  build CODE_SIGNING_ALLOWED=NO
```

Run:

```bash
chmod +x scripts/verify-ios-v1.sh
scripts/verify-ios-v1.sh
```

Expected: 所有脚本、XCTest 和 Release 模拟器构建通过。

- [ ] **Step 2: 添加 macOS GitHub Actions 工作流**

创建 `.github/workflows/verify-ios-v1.yml`：

```yaml
name: Verify Clovery Native iOS

on:
  pull_request:
    paths:
      - ".github/workflows/verify-ios-v1.yml"
      - "Clovery/**"
      - "CloveryTests/**"
      - "CloveryWidget/**"
      - "Clovery.xcodeproj/**"
      - "CloveryWidgetExtension.entitlements"
      - "Tests/**"
      - "scripts/**"
      - "docs/release/**"
  push:
    branches: [main]
    paths:
      - "Clovery/**"
      - "CloveryTests/**"
      - "CloveryWidget/**"
      - "Clovery.xcodeproj/**"
      - "CloveryWidgetExtension.entitlements"
      - "Tests/**"
      - "scripts/**"
      - "docs/release/**"

permissions:
  contents: read

concurrency:
  group: verify-ios-v1-${{ github.ref }}
  cancel-in-progress: true

jobs:
  native-ios:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Reject beta Xcode
        run: |
          xcodebuild -version
          if xcodebuild -version | grep -qi beta; then
            echo "beta Xcode is not allowed for release verification" >&2
            exit 1
          fi
      - name: Verify native iOS
        run: scripts/verify-ios-v1.sh
```

工作流使用 `macos-15` runner 的默认正式版 Xcode并记录版本；检测到 Beta 时立即失败。

- [ ] **Step 3: 在 README 记录两套验证入口**

增加：

```markdown
## Verification

- Native App Store iOS V1: `scripts/verify-ios-v1.sh`
- Go/Flutter V2 platform: `cd v2 && make verify`

The native iOS release gate must pass before Flutter feature development resumes.
```

- [ ] **Step 4: 本地运行并确认 CI 工作流语法**

Run:

```bash
scripts/verify-ios-v1.sh
git diff --check
```

Expected: 自动化通过且无 diff 格式错误。

- [ ] **Step 5: 提交 CI 门禁**

```bash
git add scripts/verify-ios-v1.sh .github/workflows/verify-ios-v1.yml README.md
git commit -m "ci: verify native iOS release candidate"
```

---

### Task 7: 建立发布证据和不可绕过的人工门禁

**Files:**
- Create: `docs/release/ios-1.0.3-acceptance.md`
- Create: `scripts/verify-ios-release-evidence.sh`
- Modify: `docs/release/1.0.3-ios-release.md`

- [ ] **Step 1: 创建非敏感验收记录**

创建 `docs/release/ios-1.0.3-acceptance.md`，固定字段如下：

```markdown
# Clovery iOS 1.0.3 Acceptance Evidence

**Commit:** `NOT_RUN`
**Archive:** `NOT_RUN`
**TestFlight build:** `NOT_RUN`

## Automated
- [ ] Native verification script — `NOT_RUN`
- [ ] Signed Release archive — `NOT_RUN`
- [ ] App and widget identity inspection — `NOT_RUN`

## Upgrade And Migration
- [ ] `1.0.2 (13)` to `1.0.3 (14)` data retention — `NOT_RUN`
- [ ] Migration counts and SHA-256 — `NOT_RUN`
- [ ] Repeated export retains both bundles — `NOT_RUN`

## Photo Library
- [ ] First authorization save — `NOT_RUN`
- [ ] Repeated save — `NOT_RUN`
- [ ] Denial and Settings recovery — `NOT_RUN`
- [ ] Share remains independent — `NOT_RUN`

## StoreKit And TestFlight
- [ ] Real App Store Connect price — `NOT_RUN`
- [ ] Cancellation — `NOT_RUN`
- [ ] Pending approval — `NOT_RUN`
- [ ] Successful purchase — `NOT_RUN`
- [ ] Relaunch and reinstall restore — `NOT_RUN`
- [ ] Second-device restore — `NOT_RUN`
- [ ] TestFlight smoke — `NOT_RUN`

## Privacy

This document contains no Apple Account, Sandbox Account, receipt, token,
transaction payload, diary text, photo, or device identifier. Restricted
screenshots and raw logs remain outside Git under `build/release-evidence/ios-1.0.3/`.
```

- [ ] **Step 2: 创建发布证据检查器**

创建 `scripts/verify-ios-release-evidence.sh`：

```sh
#!/bin/sh
set -eu

repository_root=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)
evidence="$repository_root/docs/release/ios-1.0.3-acceptance.md"

if grep -Eq '\[ \]|NOT_RUN|FAIL|BLOCKED' "$evidence"; then
  echo "iOS release evidence is incomplete or failing" >&2
  exit 1
fi

for section in Automated "Upgrade And Migration" "Photo Library" "StoreKit And TestFlight" Privacy; do
  grep -Fq "## $section" "$evidence"
done

echo "iOS 1.0.3 release evidence complete"
```

Run:

```bash
chmod +x scripts/verify-ios-release-evidence.sh
scripts/verify-ios-release-evidence.sh
```

Expected: FAIL with `iOS release evidence is incomplete or failing`. This is intentional until Tasks 8–10 produce real evidence.

- [ ] **Step 3: 将证据检查器设为最终发布门禁**

在 `docs/release/1.0.3-ios-release.md` 明确：自动化 CI 不调用该脚本；只有准备合并发布提交和创建标签时调用。任何人不得把 `NOT_RUN` 机械替换为 `PASS`，每项必须附测试时间、设备型号、系统版本和非敏感证据文件名。

- [ ] **Step 4: 提交证据框架**

```bash
git add docs/release/ios-1.0.3-acceptance.md docs/release/1.0.3-ios-release.md scripts/verify-ios-release-evidence.sh
git commit -m "docs: add iOS release evidence gate"
```

---

### Task 8: 完成自动化、签名 Archive 和产物检查

**Files:**
- Modify: `docs/release/ios-1.0.3-acceptance.md`
- Modify: `docs/release/1.0.3-ios-release.md`

- [ ] **Step 1: 运行统一自动化**

Run:

```bash
scripts/verify-ios-v1.sh
```

Expected: HTML、P0 合同、Swift 桥、迁移 ZIP、发布身份、XCTest 和 Release 模拟器构建全部 PASS。

- [ ] **Step 2: 创建签名 Release Archive**

Run:

```bash
xcodebuild \
  -project Clovery.xcodeproj \
  -scheme Clovery \
  -configuration Release \
  -destination 'generic/platform=iOS' \
  -archivePath build/Clovery-1.0.3-14.xcarchive \
  archive
```

Expected: `** ARCHIVE SUCCEEDED **`，主 App 和 Widget 均签名成功。

- [ ] **Step 3: 检查 Archive 身份与 entitlement**

Run:

```bash
plutil -p build/Clovery-1.0.3-14.xcarchive/Info.plist
codesign -d --entitlements :- build/Clovery-1.0.3-14.xcarchive/Products/Applications/Clovery.app
codesign -d --entitlements :- build/Clovery-1.0.3-14.xcarchive/Products/Applications/Clovery.app/PlugIns/CloveryWidgetExtension.appex
defaults read "$PWD/build/Clovery-1.0.3-14.xcarchive/Products/Applications/Clovery.app/Info" CFBundleShortVersionString
defaults read "$PWD/build/Clovery-1.0.3-14.xcarchive/Products/Applications/Clovery.app/Info" CFBundleVersion
```

Expected: 版本 `1.0.3`、build `14`、Bundle ID 不变，App 与 Widget 均包含 `group.com.clovery.app`；主 App 包含 `iCloud.com.clovery.app`。

- [ ] **Step 4: 写入真实自动化证据**

将 `docs/release/ios-1.0.3-acceptance.md` 的 Automated 三项改为 PASS，并记录提交 SHA、Archive 路径、执行时间和 Xcode 版本。原始 `.xcresult`、Archive 检查输出和日志保存到被 Git 忽略的 `build/release-evidence/ios-1.0.3/automated/`。

- [ ] **Step 5: 提交自动化验收记录**

```bash
git add docs/release/ios-1.0.3-acceptance.md docs/release/1.0.3-ios-release.md
git commit -m "test: record iOS automated release evidence"
```

---

### Task 9: 执行真机升级、照片和迁移验收

**Files:**
- Modify: `docs/release/ios-1.0.3-acceptance.md`
- Modify: `docs/release/w1-acceptance-2026-07-11.md`

- [ ] **Step 1: 准备受控的 `1.0.2 (13)` 基线数据**

在已解锁并保持屏幕点亮的 `Ao-iPhone` 上安装已签名基线：

```bash
xcrun devicectl device install app \
  --device "Ao-iPhone" \
  build/Clovery-W1-1.0.2-13-signed.xcarchive/Products/Applications/Clovery.app
```

在基线 App 中创建三条明确的测试日记，添加至少三张非隐私测试图片，删除其中一条形成墓碑，并确认首页、日历、详情和分享卡均可读取。

- [ ] **Step 2: 覆盖安装 `1.0.3 (14)`**

Run:

```bash
xcrun devicectl device install app \
  --device "Ao-iPhone" \
  build/Clovery-1.0.3-14.xcarchive/Products/Applications/Clovery.app
```

Expected: 安装方式为同 Bundle ID 升级，不删除 App；三条记录、三张照片和删除状态保持一致。

- [ ] **Step 3: 验证相册授权与恢复矩阵**

依次执行并录屏或截图：

1. Photos 权限未决定：点击保存，授权后系统相册出现图片。
2. 已授权：再次保存，系统相册出现第二张图片。
3. 设置中关闭“添加照片”：点击保存，不误报成功，显示“前往系统设置”。
4. 点击设置入口重新允许，返回 App 后再次保存成功。
5. 点击分享：系统分享面板正常出现，但不会改变相册保存提示。

Expected: 五项结果分别记录为 PASS；分享不能替代保存。

- [ ] **Step 4: 验证重启、CloudKit 和第二设备**

强制结束并重启 App，核对首页、日历、详情和分享卡。随后在同一测试 iCloud 账户的第二台设备上拉取 CloudKit，确认文本和照片一致；测试记录中只写设备型号和 iOS 版本，不写 Apple Account。

- [ ] **Step 5: 验证迁移 Bundle**

执行两次“设置 → 为迁移导出数据”，分别解压：

```bash
unzip -t build/release-evidence/ios-1.0.3/migration/first/migration_bundle.zip
unzip -t build/release-evidence/ios-1.0.3/migration/second/migration_bundle.zip
shasum -a 256 build/release-evidence/ios-1.0.3/migration/first/photos/*.jpg
```

Expected: manifest 条目数、墓碑数和照片数与真机样本一致；全部 SHA-256 匹配；随机三张照片可打开；两个 `migration_id` 目录都保留；返回 V1 后原数据不变。

- [ ] **Step 6: 更新并提交真机证据**

把 Upgrade And Migration 与 Photo Library 各项改为 PASS，记录日期、设备型号、iOS 版本和受限证据文件名。同步勾选 `w1-acceptance-2026-07-11.md` 中对应步骤。

```bash
git add docs/release/ios-1.0.3-acceptance.md docs/release/w1-acceptance-2026-07-11.md
git commit -m "test: record iOS device and migration evidence"
```

---

### Task 10: 完成 StoreKit、App Store Connect 和 TestFlight 验收

**Files:**
- Modify: `docs/release/ios-1.0.3-acceptance.md`
- Modify: `docs/release/w1-acceptance-2026-07-11.md`
- Modify: `docs/release/1.0.3-ios-release.md`

**Apple references:**
- `https://developer.apple.com/documentation/storekit/testing-at-all-stages-of-development-with-xcode-and-the-sandbox`
- `https://developer.apple.com/documentation/storekit/testing-ask-to-buy-in-xcode`
- `https://developer.apple.com/documentation/storekittest/sktestsession`
- `https://developer.apple.com/documentation/storekit/appstore/sync()`
- `https://developer.apple.com/help/app-store-connect/test-in-app-purchases/overview-of-testing-in-sandbox`

- [ ] **Step 1: 核对 App Store Connect 商品**

确认 `com.clovery.app.board.lifetime`：类型 Non-Consumable、参考名称、简体中文显示名称和描述、价格、中国大陆销售范围、审核截图、税务协议和版本关联全部完整。商品必须能随 `1.0.3` 提交；“趋势”页面为 0 不能单独证明购买失败，验收以商品配置、Sandbox/TestFlight 交易和设备权益结果共同判断。

- [ ] **Step 2: 上传 Archive 到 TestFlight**

在 Xcode Organizer 选择 `build/Clovery-1.0.3-14.xcarchive`，执行 Validate App 和 Distribute App → App Store Connect → Upload。上传前再次确认 Organizer 显示 `com.clovery.app`、`1.0.3 (14)` 和 Team `M92TBSSR2R`。

Expected: App Store Connect 完成 processing，build `14` 出现在 `1.0.3` TestFlight 列表，无 Bundle ID 或 entitlement 警告。

- [ ] **Step 3: 验证取消、成功购买和真实价格**

使用 Sandbox Apple Account 或 TestFlight 的 sandbox 环境：

1. 清除该 Sandbox tester 的购买历史并重新登录 Sandbox Account。
2. 打开付费页，确认显示 App Store Connect 返回的本地化真实价格。
3. 发起购买后取消，确认不解锁且不显示错误误导。
4. 再次购买并成功完成，确认立即解锁。
5. 强制结束 App 并重启，确认 `Transaction.currentEntitlements` 自动恢复权益。

- [ ] **Step 4: 验证 Pending 和交易更新监听**

分成两个不可互相替代的验收：

1. **真实 Sandbox Pending：** 用 Development 签名 App 和启动参数 `-CloverySimulateAskToBuy` 发起真实 App Store Connect 商品购买，确认 StoreKit 返回 `pending`，界面保持未解锁并显示等待批准。该参数只存在于 `#if DEBUG`，TestFlight/Release 不包含测试选项。
2. **Xcode 批准路径：** 运行 `StoreKitIntegrationTests.testAskToBuyApprovalUnlocksThroughTransactionUpdates`。测试通过 `SKTestSession.approveAskToBuyTransaction` 批准后，必须由 `Transaction.updates` 自动解锁并 finish；新建 `BoardStore` 后必须从 `currentEntitlements` 恢复。

Apple Sandbox 可以触发 Ask to Buy deferred 状态，但批准或拒绝该测试交易只能在 Xcode StoreKit 测试环境控制，因此两项证据必须分别保留。Expected: Pending 不解锁；Xcode 批准后的交易更新无需重启或再次点击购买即可解锁。

- [ ] **Step 5: 验证恢复购买、重装和第二设备**

1. 点击“恢复购买”，允许 `AppStore.sync()` 显示系统认证提示，结果为 restored。
2. 删除并从 TestFlight 重装 App，不导入本地备份，打开付费功能后当前权益自动恢复。
3. 在第二台设备登录同一 Sandbox Apple Account，安装同一 TestFlight build，确认当前权益或手动恢复购买可以解锁。
4. 使用无购买历史的 Sandbox tester 点击恢复，显示“没有找到可恢复的购买记录”，不误报失败或成功。

- [ ] **Step 6: 完成 TestFlight 全量冒烟**

验证启动、创建/编辑/删除日记、添加照片、重启、首页、日历、详情、分享卡、相册保存、权限恢复、迁移导出、价格、购买、恢复购买和 Widget。每项记录设备型号、iOS 版本、build 和 PASS/FAIL。

- [ ] **Step 7: 更新并提交 StoreKit/TestFlight 证据**

全部通过后更新三份发布文档；原始交易截图不得包含 Sandbox Account、收据或交易 JWS。

```bash
git add docs/release/ios-1.0.3-acceptance.md docs/release/w1-acceptance-2026-07-11.md docs/release/1.0.3-ios-release.md
git commit -m "test: record StoreKit and TestFlight evidence"
```

---

### Task 11: 合并、提交审核、标签和 Flutter 解锁

**Files:**
- Modify: `docs/release/ios-1.0.3-acceptance.md`
- Modify: `docs/release/1.0.3-ios-release.md`
- Modify: `docs/superpowers/plans/2026-07-10-clovery-rebuild-program.md`

- [ ] **Step 1: 运行不可绕过的最终门禁**

Run:

```bash
scripts/verify-ios-v1.sh
scripts/verify-ios-release-evidence.sh
git status --short
```

Expected: 两个脚本均 PASS，Git 工作区干净。

- [ ] **Step 2: 推送分支并等待全部 GitHub 检查**

```bash
git push origin codex/w0-platform-foundation
```

Expected: V1 macOS CI、V2 Go/Flutter CI 和发布身份检查全部绿色。

- [ ] **Step 3: 合并发布提交到 main**

通过受保护 Pull Request 合并，不改写历史。合并后检查：

```bash
git switch main
git pull --ff-only origin main
scripts/verify-ios-release-evidence.sh
```

Expected: `main` 包含实际上传到 App Store Connect 的代码和完整证据。

- [ ] **Step 4: 提交 App Store 审核**

在 App Store Connect 将 build `14` 关联到现有 `1.0.3` 版本，并按要求将首个支持该内购的版本与 `com.clovery.app.board.lifetime` 一同提交审核。审核被拒时，保持 Flutter 工作流锁定，先处理与当前 iOS 版本有关的拒审项。

- [ ] **Step 5: 创建发布标签和 GitHub Release**

在确认提交审核的 commit 与 `main` 一致后执行：

```bash
git tag -a ios-v1.0.3 -m "Clovery iOS 1.0.3"
git push origin ios-v1.0.3
gh release create ios-v1.0.3 \
  --title "Clovery iOS 1.0.3" \
  --notes "Fixes photo-library saving, purchase entitlement recovery, and V1 migration export safety."
```

- [ ] **Step 6: 解锁 Flutter 工作流**

只有 App Store Connect 接受该候选进入审核且标签指向同一提交后，才在总控计划中把 Flutter W3 标记为可开始。Flutter 继续使用 `clovery_account_id → vault_id`，不得复用 V1 设备身份、CloudKit 或 Apple Account 作为跨端数据根。

- [ ] **Step 7: 提交最终状态记录**

```bash
git switch -c codex/unlock-flutter-after-ios-1.0.3
git add docs/superpowers/plans/2026-07-10-clovery-rebuild-program.md
git commit -m "docs: unlock Flutter after iOS release"
git push origin codex/unlock-flutter-after-ios-1.0.3
```

通过新的短 Pull Request 合并该状态记录。`ios-v1.0.3` 仍指向包含完整 iOS 验收证据和实际 App Store 源代码的发布提交，不因后续 Flutter 排期文档更新而移动标签。
